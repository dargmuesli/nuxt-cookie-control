{
  "private": true,
  "name": "cookie-control-playground",
  "scripts": {
    "build": "nuxi build",
    "dev": "nuxi dev",
    "prepare": "cd .. && pnpm build --stub && cd playground && nuxi prepare"
  }
}
